
class Metric:
	def __init__(self,task_id,command,monitor_type,addr,key,val):
		self.task_id = task_id
		self.command = command
		self.monitor_type = monitor_type
		self.addr = addr
		self.key = key
		self.val = val
		
